# 1. Ocultar banner del servidor
ServerTokens Prod
ServerSignature Off

# 2. Desactivar listado de directorios y SSI
<Directory /var/www/html>
    Options -Indexes -Includes
    AllowOverride None
</Directory>

# 3. Desactivar ETag (Evita filtración de inodos)
FileETag None

# 4. Desactivar método TRACE (Previene Cross-Site Tracing)
TraceEnable Off

# 5. Limitar métodos HTTP (Solo permitir los básicos)
<Directory /var/www/html>
    <LimitExcept GET POST HEAD>
        Require all denied
    </LimitExcept>
</Directory>

# 6. Timeout bajo (Mitigación DoS lenta como Slowloris)
Timeout 60